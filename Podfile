# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '10.0'

target 'FCS Mock Season Creator' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FCS Mock Season Creator
  pod 'CardParts'
  pod 'Bond'
  pod 'PopupDialog'

end

target 'FCS Mock Season CreatorTests' do
    inherit! :search_paths
    # Pods for testing
end

target 'FCS Mock Season CreatorUITests' do
    inherit! :search_paths
    # Pods for testing
end
